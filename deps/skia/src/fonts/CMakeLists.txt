# deps/skia/src/fonts

set(SKIA_FONTS_DIR "${SKIA_DIR}/src/fonts")
# set(SKIA_FONTS_INCLUDE_DIR "${SKIA_DIR}/include/fonts")

file(GLOB SKIA_FONTS_PRIVATE_HEADERS "${SKIA_FONTS_DIR}/*.h")
file(GLOB SKIA_FONTS_SOURCES "${SKIA_FONTS_DIR}/*.cpp")
# file(GLOB SKIA_FONTS_PUBLIC_HEADERS "${SKIA_FONTS_INCLUDE_DIR}/*.h")

# list(APPEND SKIA_HEADERS ${SKIA_FONTS_PUBLIC_HEADERS})
list(APPEND SKIA_SOURCES ${SKIA_FONTS_PRIVATE_HEADERS} ${SKIA_FONTS_SOURCES})

# set(SKIA_HEADERS ${SKIA_HEADERS} PARENT_SCOPE)
set(SKIA_SOURCES ${SKIA_SOURCES} PARENT_SCOPE)
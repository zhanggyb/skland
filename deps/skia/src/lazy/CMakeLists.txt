# deps/skia/src/lazy

set(SKIA_LAZY_DIR "${SKIA_DIR}/src/lazy")
set(SKIA_LAZY_INCLUDE_DIR "${SKIA_DIR}/include/lazy")

file(GLOB SKIA_LAZY_PRIVATE_HEADERS "${SKIA_LAZY_DIR}/*.h")
file(GLOB SKIA_LAZY_SOURCES "${SKIA_LAZY_DIR}/*.cpp")
# file(GLOB SKIA_LAZY_PUBLIC_HEADERS "${SKIA_LAZY_INCLUDE_DIR}/*.h")

# list(APPEND SKIA_HEADERS ${SKIA_LAZY_PUBLIC_HEADERS})
list(APPEND SKIA_SOURCES ${SKIA_LAZY_PRIVATE_HEADERS} ${SKIA_LAZY_SOURCES})

# set(SKIA_HEADERS ${SKIA_HEADERS} PARENT_SCOPE)
set(SKIA_SOURCES ${SKIA_SOURCES} PARENT_SCOPE)

# deps/skia/src/shaders/gradients

set(SKIA_SHADERS_GRADIENTS_DIR "${SKIA_DIR}/src/shaders/gradients")
# set(SKIA_SHADERS_GRADIENTS_INCLUDE_DIR "${SKIA_DIR}/include/shaders/gradients")

file(GLOB SKIA_SHADERS_GRADIENTS_PRIVATE_HEADERS "${SKIA_SHADERS_GRADIENTS_DIR}/*.h")
file(GLOB SKIA_SHADERS_GRADIENTS_SOURCES "${SKIA_SHADERS_GRADIENTS_DIR}/*.cpp")
# file(GLOB SKIA_SHADERS_GRADIENTS_PUBLIC_HEADERS "${SKIA_SHADERS_GRADIENTS_INCLUDE_DIR}/*.h")

# list(APPEND SKIA_HEADERS ${SKIA_SHADERS_GRADIENTS_PUBLIC_HEADERS})
list(APPEND SKIA_SOURCES ${SKIA_SHADERS_GRADIENTS_PRIVATE_HEADERS} ${SKIA_SHADERS_GRADIENTS_SOURCES})

# set(SKIA_HEADERS ${SKIA_HEADERS} PARENT_SCOPE)
set(SKIA_SOURCES ${SKIA_SOURCES} PARENT_SCOPE)

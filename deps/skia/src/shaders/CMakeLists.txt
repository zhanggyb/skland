# deps/skia/src/shaders

set(SKIA_SHADERS_DIR "${SKIA_DIR}/src/shaders")
# set(SKIA_SHADERS_INCLUDE_DIR "${SKIA_DIR}/include/shaders")

file(GLOB SKIA_SHADERS_PRIVATE_HEADERS "${SKIA_SHADERS_DIR}/*.h")
file(GLOB SKIA_SHADERS_SOURCES "${SKIA_SHADERS_DIR}/*.cpp")
# file(GLOB SKIA_SHADERS_PUBLIC_HEADERS "${SKIA_SHADERS_INCLUDE_DIR}/*.h")

# list(APPEND SKIA_HEADERS ${SKIA_SHADERS_PUBLIC_HEADERS})
list(APPEND SKIA_SOURCES ${SKIA_SHADERS_PRIVATE_HEADERS} ${SKIA_SHADERS_SOURCES})

add_subdirectory(gradients)

# set(SKIA_HEADERS ${SKIA_HEADERS} PARENT_SCOPE)
set(SKIA_SOURCES ${SKIA_SOURCES} PARENT_SCOPE)

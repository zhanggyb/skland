# deps/skia/src/image

set(SKIA_IMAGE_DIR "${SKIA_DIR}/src/image")
# set(SKIA_IMAGE_INCLUDE_DIR "${SKIA_DIR}/include/image")

file(GLOB SKIA_IMAGE_PRIVATE_HEADERS "${SKIA_IMAGE_DIR}/*.h")
file(GLOB SKIA_IMAGE_SOURCES "${SKIA_IMAGE_DIR}/*.cpp")
# file(GLOB SKIA_IMAGE_PUBLIC_HEADERS "${SKIA_IMAGE_INCLUDE_DIR}/*.h")

# list(APPEND SKIA_HEADERS ${SKIA_IMAGE_PUBLIC_HEADERS})
list(APPEND SKIA_SOURCES ${SKIA_IMAGE_PRIVATE_HEADERS} ${SKIA_IMAGE_SOURCES})

# set(SKIA_HEADERS ${SKIA_HEADERS} PARENT_SCOPE)
set(SKIA_SOURCES ${SKIA_SOURCES} PARENT_SCOPE)
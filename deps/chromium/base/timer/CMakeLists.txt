# deps/chromium/base/timer

set(CHROMIUM_BASE_TIMER_DIR "${CHROMIUM_BASE_DIR}/timer")

file(GLOB CHROMIUM_BASE_TIMER_HEADERS "${CHROMIUM_BASE_TIMER_DIR}/*.h")
file(GLOB CHROMIUM_BASE_TIMER_SOURCES "${CHROMIUM_BASE_TIMER_DIR}/*.cc")

# Remove sources for other platform or unit test:
remove_file(CHROMIUM_BASE_TIMER_SOURCES GLOB "${CHROMIUM_BASE_TIMER_DIR}/*_unittest.cc")
#file_list_remove(CHROMIUM_BASE_TIMER_SOURCES GLOB "${CHROMIUM_BASE_TIMER_DIR}/*_mac.cc")
remove_file(CHROMIUM_BASE_TIMER_SOURCES GLOB "${CHROMIUM_BASE_TIMER_DIR}/*_win.cc")
#file_list_remove(CHROMIUM_BASE_TIMER_SOURCES GLOB "${CHROMIUM_BASE_TIMER_DIR}/*_fuchsia.cc")

list(APPEND CHROMIUM_BASE_HEADERS ${CHROMIUM_BASE_TIMER_HEADERS})
list(APPEND CHROMIUM_BASE_SOURCES ${CHROMIUM_BASE_TIMER_SOURCES})

set(CHROMIUM_BASE_HEADERS ${CHROMIUM_BASE_HEADERS} PARENT_SCOPE)
set(CHROMIUM_BASE_SOURCES ${CHROMIUM_BASE_SOURCES} PARENT_SCOPE)